"""
Bronze Layer Configuration

This module stores configuration values used by the Bronze ingestion
pipeline, including file paths, table names, partition columns, and
other pipeline constants. Keeping configuration separate from business
logic simplifies maintenance and deployment across environments.
"""